# Normally set for you: `npm run dev` (root) injects local values, and
# `npm run deploy:admin` injects the deployed stack's values.
VITE_API_URL=/api
VITE_COGNITO_USER_POOL_ID=eu-north-1_xxxxxxxxx
VITE_COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
